# CHANGELOG

## Unreleased

- 火山方舟 Seedream 图片生成显式关闭“AI 生成”水印，并由后端下载方舟 CDN 结果后写入服务器或对象存储，恢复素材库自动入库。
- 项目开源协议由 AGPL-3.0 调整为 MIT，保留上游及项目贡献者版权声明。
- 修复任务中心等资源入口仍由应用服务器转发对象存储媒体的问题：配置 CDN 加速域名后，登录资源、公开画布分享和管理员媒体预览统一重定向到 CDN；内部 Blob 读取保留同源代理兼容。

## v1.0.59

- 画布媒体工作流支持批量连线、失败重试与模型能力增强。
- 修复 PostgreSQL 素材同步无法保存稳定生成 ID 的问题，素材主键及关联字段扩容至 80 个字符，并增加输入边界校验。
- 项目贡献者列表新增 dyh 与 kyori 资料。

## v1.0.58

- 管理后台“功能开放”新增自定义渠道开关，关闭后保留用户已有配置，但隐藏配置入口、过滤自定义模型，并拦截模型目录拉取、渠道中转与新的异步生成任务。
- 阿里云 OSS 与腾讯云 COS 的平台、个人存储支持配置可选 CDN 加速域名，上传继续使用 Endpoint，下载与预览在配置后改用 CDN 域名。
- 新增即梦本机运行时与桌面本机渠道，建立本地会话、任务持久化、状态协调和安全生成闭环。
- 火山方舟视频任务支持按真实 Token 预授权与结算，素材库展示账号文件容量和使用进度。
- 优化工作区启动与路由切换状态，避免页面切换时重复出现全屏加载，并收敛积分摘要请求。

## v1.0.57

- 修复已有媒体节点再次生成失败时污染原成功节点状态的问题，视频新版本仅在生成成功后切换为主版本。
- 修复画布视频模型因内部标识缺少视频关键词而回退到默认模型的问题，模型选择统一以渠道协议和后台能力配置为准。

## v1.0.56

- 画布提示词输入支持稳定的自动增高、手动拖拽和模型配置回显，重构 `@` 引用菜单为已连接节点、技能库和素材库三类紧凑入口。
- 画布在线 Agent、本地 Agent 与创作对话统一使用面向 AI 流式输出的 Markdown 渲染，支持 GFM、代码高亮、未闭合 Markdown 解析和安全链接处理。

## v1.0.55

- 重构画布节点、提示词编辑器和顶部 Dock，统一图片与视频工具层级、节点尺寸及低干扰网格视觉，并拆分超长节点内容组件。
- 收敛任务中心与设置页的状态筛选、卡片表面和交互反馈，移除重复入口、装饰描边、阴影及状态胶囊。
- 修复时间线导出的黑场顺序、无源片段对齐与裁切偏移，支持精确裁切视频和提取音轨，并补齐 Grok 图片宽高比用例。

## v1.0.54

- 统一工作台卡片、按钮、开关、素材选择器和弹窗视觉，移除装饰竖线、冗余分割线及重复样式，并补充明暗主题交互约束。

## v1.0.53

- 修复项目资产统一选择器的联合类型推断错误，恢复前端生产构建。

## v1.0.52

- 优化创作页首次加载：合并用户画布与素材快照请求、复用积分余额查询，并按路由延迟加载页面代码。
- 统一创作页、自由画布和项目资产的素材选择器，重构节点信息面板与创作入口卡片样式。
- 修复画布顶栏项目入口过宽、长文本溢出，以及暗色主题下按钮、选项、勾选框和焦点态对比错误。

## v1.0.51

- 收敛用户工作台、短剧创建区、画布列表与创作对话的边框和分割线，统一导航图标、卡片表面层级、列表间距及内容宽度。

## v1.0.50

- 新增腾讯云 COS 私有对象存储，支持平台和个人存储配置、对象上传、Range 读取与短时签名 URL，并保留原有阿里云 OSS 链路。
- 新增 Novita 异步视频生成协议，并修复 NewAPI 纯音频请求、Grok 图片尺寸参数和火山方舟最小像素尺寸兼容问题。
- 图片生成按模型能力限制参考图数量，模型选择合并同名项并根据输入动态约束画布节点连接。
- 系统渠道密钥改为加密落库并在运行时解密，补齐安全配置与资源存储测试。
- 重构创作工作区、项目、素材和任务中心页面，完善任务列表信息层级与画布模型交互。
- 增强角色提取对代码块、前缀文本和有效 JSON 的解析能力，并升级后端与 Canvas Agent 依赖。

## v1.0.49

- 创作首页与画布提示词引用图支持点击放大，并为新素材上传补充落库说明、进行中状态和完成反馈。
- 修复分镜视频节点的模型同步和视频端点协议识别，模型目录可按上游端点能力纠正协议，同时增强分镜结构的有界自动修复。
- 修复运营后台测试图片模型时硬编码尺寸和质量的问题，改为使用模型能力声明的默认参数。

## v1.0.48

- 移除图片、视频和画布生成链路的本地提示词长度限制，完整提示词交由上游模型判断。

## v1.0.47

- 修复画布视频节点自动拼接上游文本、角色描述、技能指令、项目画风和分镜模板导致提示词超限的问题，视频生成与重试现在只使用输入框内容。

## v1.0.46

- 新增批量文件选择、拖拽上传和图片/视频预览，支持在提交前单项移除文件。
- 新增实时对话的行内语音录制与浏览器语音转写，并补充本地录音测试页面。
- 新增 Grok JSON 生图与单图编辑协议，修复自部署视频合并的同源 FFmpeg 加载问题。
- 重构分镜脚本节点的表格/卡片视图和流程信息层级，修复滚动条 gutter 与列线对齐问题。
- 修复分镜生成流式保活与长任务超时，避免长时间任务提前失败。

## v1.0.45

- 新增项目级画风方案、参考素材绑定与生成执行链路，并支持按图片模型能力裁剪可用参数。
- 重构画布卡片层级与交互动效，统一创作工作区的卡片视觉和操作反馈。
- 修复角色提取对带推理文本 JSON 的兼容问题，并修正锁文件、质量门禁与生成链路的生产构建失败。

## v1.0.44

- 新增模型级视频能力、限制校验与请求协议管理，并将渠道模型的能力与计费配置收敛到单模型抽屉。
- 收敛后端业务 API 客户端、模型职责和异步计费巡检，补充持续集成质量检查与仓储边界约束。
- 重构用户工作台、画布、任务和资源页面，统一项目画布与资产卡片，并恢复资源预览、角色交互和主题辨识度。
- 修复活动任务面板遮挡顶部工具，以及视频参考音频缺少文件大小类型导致的前端生产构建失败。

## v1.0.43

- 修复视频结果下载缺少 Grok 同源认证，确保同源资源可以正常获取。

## v1.0.42

- 补齐 SQLite 到 PostgreSQL 迁移工具的结构化资产与工作流表清单，避免跨库迁移覆盖校验失败。

## v1.0.41

- 文本事件流支持断线恢复与增量清理，统一 PostgreSQL 增量表名并补齐跨库迁移清单。
- 新增画布沉浸专注模式、短剧流程与图片裁剪比例控制，完善任务取消传播和费用对账。
- 升级前后端关键依赖，并补充文本事件流部署反向代理指南。

## v1.0.40

- 修复导演台角色、动画清理、骨骼控制与白膜参考视频类型错误导致的生产构建失败。

## v1.0.39

- 创作任务支持页面切换后继续后台执行和及时停止，并保证任务结果回填与素材入库幂等。
- 修复文本回放增量配额与生命周期清理，并按真实 Token 用量结算预授权差额。
- 音频任务支持异步 Provider 协议与结果校验，完善媒体生成链路。
- 导演台新增彩色骨骼演员与姿势预设，统一基础人偶初始化、缩小 Dock 并保持单一构图回写。
- 修复添加节点菜单、画布工具栏、模型悬浮菜单和图生图比例转换等交互问题。

## v1.0.38

- 修复匿名签名资源下载缺少 repository 查询方法导致的后端生产构建失败。

## v1.0.37

- 管理后台模型能力与请求协议改为品牌卡片单选，提升配置辨识度与操作一致性。
- 本地媒体存储支持自定义访问地址和短时签名链接，并恢复服务器本地与阿里云 OSS 的显式选择。
- 修复浅色模式下发送按钮悬停反馈不可见、侧栏分界不清及可点击元素缺少指针样式的问题。

## v1.0.36

- 修复暗色主题下 shadow token 使用黑色阴影导致层级不可见的问题，改为白色微影。

## v1.0.35

- 重构画布浮层层级、宽度与边距 token，完善专注模式和工具栏点击触发交互。
- 清理硬编码字号与圆角，收紧用户工作区素材卡片和分页布局，并统一设置页表面。
- 修复提示词偏好在 React StrictMode 下的重复请求竞态，并补充加载失败错误态。
- 赞助商展示新增个人、团队与商业分类，补充快乐机艺术小组资料并同步版本徽章。

## v1.0.34

- 修复提示词编辑读取可选节点元数据时导致的前端生产构建失败。

## v1.0.33

- 新增功能开放运维开关和管理员添加用户能力，并优化后台、创作页与移动端页面布局。
- 重构分镜画风、提示词模板和视频生成链路，统一画布工具栏注册、上下文感知、自定义排序与危险操作隔离。
- 建立画布三层设计 Token、五层面板层级、动效和响应式体系，完善无障碍与节点视觉表现。
- 修复 HTTP 内网 IP 访问时 `crypto.randomUUID` 不可用导致创造页卡死，并统一画布、Agent 与生成请求的客户端 ID 生成方式。
- 画布绘图节点支持 tldraw 与 Excalidraw 双引擎，管理员可配置新建默认工具，并保持历史绘图和导入导出兼容。
- 修复创作页文本流重复拼接、人物质感变体缩略图不更新等问题。
- 新增 Docker 开发环境热更新，并完善私网上游配置指引。

## v1.0.32

- 修复 GORM 技能总数查询将 `DISTINCT` 状态带入后续列表查询，导致 PostgreSQL 热门排序仍返回 `SQLSTATE 42P10` 的问题。

## v1.0.31

- 修复分镜脚本节点文字溢出与视频生成模型选择器下拉异常。
- 创作页图片与视频任务支持刷新后从任务中心恢复，批量任务会按关联消息聚合结果和失败状态。
- 创作输入支持普通回车换行、`Ctrl/Cmd + Enter` 发送，并完善参考图片与生成媒体的预览交互。
- 修复 PostgreSQL 下技能库热门排序因冗余 `DISTINCT` 导致列表接口返回 `SQLSTATE 42P10` 的问题。
- 更新 README 的登录注册、AI 创作、项目制作概览与自由画布界面预览。

## v1.0.30

- 创作区 `@` 引用仅展示本次上传的参考图片和已加入技能，上传图片按顺序显示友好编号，并加宽候选浮层避免长内容换行。
- 修复参考图任务携带页面预览 Data URL 导致后端拒绝提交的问题，媒体任务输入现只保留后端协议字段。

## v1.0.29

- 修复模型选择器重构遗漏创建版耗时标签条件导致的前端生产构建失败。

## v1.0.28

- 创作区支持通过 `@` 引用本次上传图片、素材库资源和已加入技能，并将图片、视频、音频参考内容传入统一生成任务；失败重试会撤回原记录并恢复原提示词、引用和生成参数。
- 统一清洗创作对话、画布节点和任务反馈中的上游错误，隐藏请求地址、网络报文和系统渠道原始模型 ID，同时保留后端原始日志供排查。
- 优化画布列表为全宽自适应媒体网格，重整卡片信息层级、筛选工具带和分页布局，减少宽屏空白与内容遮挡。

## v1.0.27

- 修复通用生成任务重构遗漏模型请求配置解析依赖导致的前端生产构建失败。

## v1.0.26

- 修复统一创作页图片和视频绕过后端任务系统的问题，生成任务现统一记录到任务中心并纳入积分预占、结算、退款和取消流程。
- 修复创作区长会话滚动条缺失和消息不自动跟随的问题，流式输出与媒体结果回填会在用户停留底部时持续定位最新消息。

## v1.0.25

- 修复统一创作页媒体结果可选字段未正确收窄导致的前端生产构建失败。

## v1.0.24

- 新增统一多模态创作页，支持文本、图片、视频模式、历史会话和视频参数控制。
- 重写技能库为后端持久化的用户共享能力，支持创建、公开、加入、收藏和画布引用，并初始化 33 个内置公开技能，不再依赖 Updream 远程社区。
- 优化工作台侧边栏的滚动提示与认证页视觉，登录、注册及 Linux.do OAuth 在无指定返回地址时默认进入创作页。

## v1.0.23

- 修复视频 Composer 丢失已配置首尾帧的问题，并按 `POST /v1/video/generations` 协议顺序传递首帧、尾帧和普通参考图。
- 修复图片批次切换主图时媒体存储元数据未同步的问题，并为当前主图增加明确选中状态。

## v1.0.22

- 提升提示词卡片模型菜单的可读性：使用不透明主题表面，并以黄色粗体突出积分价格。

## v1.0.21

- 修复 NewAPI 视频任务提前超时：统一服从后台最长执行时间，网络、限流及上游错误按分钟重试，并支持失败任务按原任务 ID 手动恢复和正常计费。
- 修复模型选择项描述过长导致横向溢出的问题。
- 重构画布 Agent 对话区，收敛顶栏操作、分段导航、消息与工具状态、空状态和输入区层级，并优化窄面板布局。

## v1.0.20

- 修复图片生成显式比例被参考图节点尺寸覆盖的问题，重试和任务恢复时保持用户选择的比例。
- 优化画布提示词面板、模型选择器和节点工具弹窗交互，降低节点缩放与视口更新时的卡顿。

## v1.0.19

- 修复 Vidstack 主题色 CSS 变量类型不兼容导致的前端生产构建失败。

## v1.0.18

- 视频节点改用 Vidstack 播放器，补充中文控制界面、媒体类型识别和稳定加载，并避免紧凑画布节点的控制按钮遮挡标题与存储状态。
- 画布生成批次仅在存在等待项时查询当前画布的活跃任务，刷新恢复也按画布筛选，减少任务接口的全量轮询。

## v1.0.17

- 用户工作台与画布入口统一为黑白灰空间界面，收敛浮层、模型选择和输入焦点样式。
- 视频节点支持自定义时长、常用时长快捷选项和分辨率设置，并修正 Video Generations 时长字段类型。
- 画布节点新增外置可编辑标题与侧边跟随锚点快速连线，图生图自动结果继承来源图片尺寸。
- 提示词面板统一黑色层级、弱化生硬边框，并移除输入区域的聚焦描边。
- 系统公告支持重新发布，画布阻止浏览器误缩放。

## v1.0.16

- 画布引入 LeaferJS 图形层，提升缩放与平移过程中的渲染清晰度和性能。
- 图片生成会自动避让已有节点，基于已有图片重新生成时保留原结果并创建新节点。
- 管理后台删除渠道模型后支持重新拉取同名模型，并对真实重名冲突提供明确提示。

## v1.0.15

- 自定义渠道支持配置额外请求头，并统一应用到文本、图片、视频等上游请求。
- 图片节点新增人物质感调节，可基于来源图片配置人景融合、光影、皮肤、纹理与锐度并生成结果。
- 图片节点“更多”菜单支持直接执行未固定工具，同时保留最多 7 个快捷工具的固定管理能力。

## v1.0.14

- 系统模型协议新增火山方舟与即梦图片、视频生成，支持 AK/SK 凭证、协议能力校验和异步任务轮询。
- 运维后台支持在新增模型前测试真实连接，统一上游请求 User-Agent，并补充自定义渠道音频生成路径。
- 积分运营支持批量确认计费订单扣费或退回积分，逐单记录处理结果和审计信息。
- 修复画布缩放或平移时模型下拉框锚点不同步，以及火山 SDK 传递依赖校验信息缺失导致的后端镜像构建失败。

## v1.0.13

- 运维后台和用户自定义渠道支持为每个模型独立选择请求协议，并明确展示创建路径、Content-Type、轮询和素材要求。
- 修复 ArtBox Artdance 视频错误使用 multipart `/v1/videos` 的问题，NewAPI Video Generations 改用 JSON `/v1/video/generations` 并支持图片、视频、音频 OSS 签名地址；补充 xAI 与 Gemini Veo 长任务协议。
- 请求明细记录实际 Content-Type，便于区分 JSON 与 multipart 请求报文。
- 修复视频协议响应类型过宽导致的前端发布构建失败。

## v1.0.12

- 画布退役生成配置节点的新建入口，文本生图、图片反推、导演台及 Agent 编排改为直接创建对应类型的生成目标节点。
- 历史生成配置节点继续保持读取和执行兼容，直接文本目标支持按数量参数批量生成结果。

## v1.0.11

- 修复 PostgreSQL 请求明细分页继承 `DISTINCT` 后与创建时间排序冲突的问题，恢复管理后台列表读取。
- 请求明细列表支持预览和下载图片、视频结果，私有媒体通过管理员鉴权流接口读取并支持视频 Range 请求。

## v1.0.10

- 运维后台请求明细按真实调用聚合视频轮询，补充用户账号、状态、错误、耗时、计费和 Token，并仅在详情中展示脱敏后的请求与响应报文。
- 修复请求明细用户信息装配函数返回值不匹配导致的后端构建失败。
- 分镜脚本 Composer 支持选择并保存文本模型，首次生成和失败重试均使用节点所选模型。

## v1.0.9

- 修复 Seedance `/videos` 请求遗漏声音生成参数的问题，启用声音时生成结果会包含上游音轨。
- tldraw License Key 改由前端镜像构建参数注入，服务器部署可通过环境配置更新授权。

## v1.0.8

- 项目画风扩展为 18 套预设，支持查看完整规范，并在生成角色三视图时引用当前已保存画风。

## v1.0.7

- README 增加临时演示环境入口与测试账号说明，便于外部演示与验收。

## v1.0.6

- 账号活动任务上限统一跟随后台热配置，任务中心默认展示全部任务，并在创建或重试后自动切换到运行中记录。
- 修复分镜脚本失败重试误调用图片模型的问题，支持从文本节点连线创建分镜脚本并继承剧情内容。
- 视频节点角色引用改用稳定节点标识，角色三视图、视觉设定、声音描述及渠道支持的声音样本可随视频任务和重试共同提交。
- 角色引用架与行内标签优先预览人物三视图，避免显示文档或文字占位图标。

## v1.0.5

- 新增短剧项目空间与结构化资产创作闭环，支持项目、章节、角色、镜头和画布资产协同管理。
- 管理员可从上游渠道一键拉取模型目录；缺失模型自动导入为停用待定价，避免浏览器直连上游时的 CORS 问题。
- 管理后台新增统一资源与请求策略配置，支持默认重置、自用模式、即时生效和用户配额用量查看。
- 画布媒体支持复制私有 OSS 签名地址，图片生成支持透明背景与 21:9 尺寸。
- 修复音视频持久化恢复与短剧项目空间合并引发的 Web 构建错误。
- 镜像安装部署可显式复用既有后端、PostgreSQL 和 Redis 数据目录。

## v1.0.4

- 系统后台支持热更新 Worker 与全局渠道并发，各系统渠道可跟随全局值或单独覆盖；渠道满载时请求等待空闲槽位，不再直接失败。
- 加宽画布图片、视频模型选择器及弹出菜单，减少长模型名称和说明被截断。

## v1.0.3

- 系统公告正文支持受控 HTML，用户可以看到蓝色链接并在新窗口打开，同时支持粗体、斜体、下划线、列表和换行等常见格式。
- 修复本地 Codex 会话新建后首次发送失败的问题。

## v1.0.2

- 增加前端与后端多架构 GHCR 镜像发布，并让服务器一键部署直接拉取指定版本镜像。
- 完善 Codex App 插件的仓库 marketplace 安装入口。
- 修复 xAI 视频生成请求未使用官方字段的问题。

## v1.0.1

- 自定义模型渠道改由登录态后端中转，减少第三方接口 CORS 限制。
- 增加 xAI 与 Sub2API 的 Grok 视频生成协议支持。
- 增加私有上游精确主机白名单，收紧自定义渠道访问边界。
- 修复 Agent 会话未展示后端具体失败原因的问题。
- 修复主页删除最近项目时未显示确认弹窗的问题。
- 修复 PostgreSQL 用户活跃统计冲突更新时的列歧义问题。
